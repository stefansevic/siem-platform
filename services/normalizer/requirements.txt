# Production dependencies for the Normalizer service.
# Versions pinned to match the local development environment so Docker
# builds are reproducible.

# Core
pydantic==2.13.3
pydantic-settings==2.14.0

# Postgres (async)
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0

# Redis (async included in main package since 4.2.x)
redis==7.4.0
