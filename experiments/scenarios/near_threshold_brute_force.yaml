name: near_threshold_brute_force
description: |
  Granični slučaj: 4 neuspela login-a (jedan ispod brute_force praga 5).
  SIEM NE sme da detektuje ovo kao napad. Proverava da je logika praga
  tačna i da ne okida ispod praga.

expected_incidents: []

steps:
  - type: attack
    script: brute_force.py
    args:
      - --username
      - alice
      - --attempts
      - "4"
      - --delay
      - "0.5"
  - type: wait
    seconds: 5